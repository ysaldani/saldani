<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yassine Saldani - Computer Programmer</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> 
<style>
        /* Base Styles & Variables */
        :root {
            --primary-color: #007BFF; /* Blue */
            --secondary-color: #28a745; /* Green for WhatsApp */
            --dark-text: #343a40;
            --light-text: #6c757d;
            --bg-light: #f8f9fa;
            --card-bg: #ffffff;
            --font-family: 'Poppins', sans-serif;
            --border-radius: 12px; /* Slightly larger radius */
            --shadow: 0 4px 15px rgba(0,0,0,0.1);
            --transition-speed: 0.3s ease;
        }

        body {
            font-family: var(--font-family);
            background-color: var(--bg-light);
            color: var(--dark-text);
            line-height: 1.7;
            margin: 0;
            padding: 0;
            direction: ltr; 
            text-align: left;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header / Hero Section */
        .hero-section {
            background: linear-gradient(135deg, var(--primary-color) 0%, #0056b3 100%);
            color: white;
            padding: 80px 20px;
            text-align: center;
            border-radius: var(--border-radius);
            margin-bottom: 40px;
            box-shadow: 0 10px 25px rgba(0, 123, 255, 0.3); /* Stronger shadow for emphasis */
        }

        .profile-image-container {
            width: 160px; /* Slightly larger image */
            height: 160px;
            border-radius: 50%;
            overflow: hidden;
            margin: 0 auto 20px;
            border: 6px solid rgba(255,255,255,0.4);
            box-shadow: 0 0 0 10px rgba(255,255,255,0.2);
        }

        .profile-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }

        .hero-section h1 {
            font-size: 3.5em;
            margin-bottom: 5px;
            font-weight: 700;
        }

        .hero-section .subtitle {
            font-size: 1.4em;
            margin-bottom: 20px;
        }

        .hero-section p {
            font-size: 1.1em;
            opacity: 0.9;
            max-width: 750px;
            margin: 0 auto;
        }

        /* Section Headings */
        h2 {
            font-size: 2.5em;
            color: var(--dark-text);
            text-align: center;
            margin-bottom: 50px;
            font-weight: 700;
        }
        
        h2::after {
            content: '';
            display: block;
            width: 80px;
            height: 5px;
            background: var(--primary-color);
            margin: 15px auto 0;
            border-radius: 3px;
        }

        /* Feature Cards Section */
        .features-section {
            padding: 40px 0;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            justify-content: center;
        }

        .feature-card {
            background-color: var(--card-bg);
            border: 1px solid #e9ecef;
            border-radius: var(--border-radius);
            padding: 30px;
            box-shadow: var(--shadow);
            transition: transform var(--transition-speed), box-shadow var(--transition-speed);
            text-align: center;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 25px rgba(0,0,0,0.15);
        }

        .feature-card .icon-wrapper {
            font-size: 3.5em;
            color: var(--primary-color);
            margin-bottom: 15px;
            display: block;
        }

        .feature-card h3 {
            color: var(--dark-text);
            font-size: 1.6em;
            margin-top: 0;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .feature-card p {
            color: var(--light-text);
            font-size: 1em;
        }
        
        /* Call to Action */
        .cta-text {
            text-align: center;
            margin-top: 50px;
            font-size: 1.3em;
            color: var(--dark-text);
            font-weight: 600;
        }
        .cta-text strong {
            color: var(--primary-color);
        }

        /* Contact Section */
        .contact-section {
            background-color: var(--card-bg);
            padding: 50px 20px;
            text-align: center;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            margin-top: 50px;
        }

        .contact-section p {
            margin-bottom: 30px;
            font-size: 1.1em;
        }

        .contact-info {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 25px;
        }

        .contact-info a {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 18px 30px; /* Larger padding for better click area */
            text-decoration: none;
            color: white;
            border-radius: 35px; 
            font-weight: 600;
            font-size: 1.15em;
            transition: background-color var(--transition-speed), transform var(--transition-speed);
            box-shadow: 0 6px 15px rgba(0,0,0,0.15);
        }

        .contact-info a i {
            margin-right: 12px;
            font-size: 1.3em;
        }

        .email-btn {
            background-color: var(--primary-color);
        }
        
        .email-btn:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
        }

        .whatsapp-btn {
            background-color: var(--secondary-color);
        }

        .whatsapp-btn:hover {
            background-color: #1e7e34;
            transform: translateY(-3px);
        }
        
        /* Footer */
        footer {
            text-align: center;
            padding: 40px 20px;
            font-size: 0.95em;
            color: var(--light-text);
            margin-top: 60px;
            border-top: 1px solid #e9ecef;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero-section h1 {
                font-size: 3em;
            }
            .hero-section .subtitle {
                font-size: 1.2em;
            }
            h2 {
                font-size: 2.2em;
            }
            .feature-grid {
                grid-template-columns: 1fr; 
            }
            .contact-info a {
                min-width: 90%; 
            }
        }
    </style>
</head>
<body>

    <div class="container">
        
        <section class="hero-section">
            <div class="hero-content">
                <div class="profile-image-container">
                    <img src="https://avatars.githubusercontent.com/u/12399793?v=4" alt="Yassine Saldani Profile" class="profile-image">
                </div>
                <h1>Yassine Saldani</h1>
                <p class="subtitle">
                    **Computer Programmer specializing in software development and IT solutions.**
                </p>
                <p>
                    I translate ambitious concepts into robust, scalable applications and cutting-edge technical architectures, focusing on maximizing business efficiency and innovation.
                </p>
            </div>
        </section>

        <section class="features-section">
            <h2>My Core Expertise</h2>
            <div class="feature-grid">
                
                <div class="feature-card">
                    <span class="icon-wrapper"><i class="fas fa-laptop-code"></i></span>
                    <h3>Bespoke Software Development</h3>
                    <p>Crafting powerful, bespoke applications that solve complex problems and integrate seamlessly with existing business ecosystems.</p>
                </div>
                
                <div class="feature-card">
                    <span class="icon-wrapper"><i class="fas fa-project-diagram"></i></span>
                    <h3>IT Strategy & Consulting</h3>
                    <p>Delivering strategic technical advice and implementing streamlined IT solutions to ensure optimal performance and future-proof your infrastructure.</p>
                </div>
                
                <div class="feature-card">
                    <span class="icon-wrapper"><i class="fas fa-cubes"></i></span>
                    <h3>End-to-End Full-Stack</h3>
                    <p>Expertise across the entire development lifecycle, from elegant front-end user interfaces to highly efficient and secure back-end services.</p>
                </div>
            </div>
            
            <p class="cta-text"><strong>Ready to build something impactful? Contact me today.</strong></p>

        </section>
        
        <section class="contact-section">
            <h2>Connect with Yassine</h2>
            <p>I am available for consultation and ready to begin work on innovative projects.</p>
            <div class="contact-info">
                
                <a href="mailto:ysnsaldani@gmail.com" class="email-btn">
                    <i class="fas fa-envelope"></i> ysnsaldani@gmail.com
                </a>
                
                <a href="https://wa.me/447933365126" target="_blank" class="whatsapp-btn">
                    <i class="fab fa-whatsapp"></i> +447933365126
                </a>
            </div>
        </section>

    </div>
    
    <footer>
        &copy; 2025 Yassine Saldani | Computer Programmer.
    </footer>

</body>
</html>
