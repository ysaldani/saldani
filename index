<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yassine Saldani - Computer Programmer</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <style>
        /* Base Styles & Variables */
        :root {
            --primary-color: #007BFF; /* Blue */
            --secondary-color: #28a745; /* Green for WhatsApp */
            --dark-text: #343a40;
            --light-text: #6c757d;
            --bg-light: #f8f9fa;
            --card-bg: #ffffff;
            --font-family: 'Poppins', sans-serif;
            --border-radius: 8px;
            --shadow: 0 4px 12px rgba(0,0,0,0.08);
            --transition-speed: 0.3s ease;
        }

        body {
            font-family: var(--font-family);
            background-color: var(--bg-light);
            color: var(--dark-text);
            line-height: 1.7;
            margin: 0;
            padding: 0;
            direction: ltr; /* Ensure left-to-right for English */
            text-align: left;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header / Hero Section */
        .hero-section {
            background: linear-gradient(135deg, var(--primary-color) 0%, #0056b3 100%);
            color: white;
            padding: 80px 20px;
            text-align: center;
            border-radius: var(--border-radius);
            margin-bottom: 40px;
            position: relative;
            overflow: hidden; /* For wave effect if added */
        }

        .hero-content {
            z-index: 10;
            position: relative;
        }

        .profile-image-container {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            overflow: hidden;
            margin: 0 auto 20px;
            border: 5px solid rgba(255,255,255,0.3);
            box-shadow: 0 0 0 8px rgba(255,255,255,0.2);
        }

        .profile-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }

        .hero-section h1 {
            font-size: 3em;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .hero-section p {
            font-size: 1.3em;
            opacity: 0.9;
            max-width: 700px;
            margin: 0 auto;
        }

        /* Section Headings */
        h2 {
            font-size: 2.5em;
            color: var(--primary-color);
            text-align: center;
            margin-bottom: 40px;
            position: relative;
            font-weight: 600;
        }
        
        h2::after {
            content: '';
            display: block;
            width: 70px;
            height: 4px;
            background: var(--primary-color);
            margin: 15px auto 0;
            border-radius: 2px;
        }

        /* Feature Cards Section */
        .features-section {
            padding: 40px 0;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            justify-content: center;
        }

        .feature-card {
            background-color: var(--card-bg);
            border: 1px solid #e9ecef;
            border-radius: var(--border-radius);
            padding: 30px;
            box-shadow: var(--shadow);
            transition: transform var(--transition-speed), box-shadow var(--transition-speed);
            text-align: center;
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.12);
        }

        .feature-card .icon-wrapper {
            font-size: 3em;
            color: var(--primary-color);
            margin-bottom: 15px;
            display: block;
        }

        .feature-card h3 {
            color: var(--primary-color);
            font-size: 1.5em;
            margin-top: 0;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .feature-card p {
            color: var(--light-text);
            font-size: 1em;
        }
        
        /* Call to Action */
        .cta-text {
            text-align: center;
            margin-top: 50px;
            font-size: 1.2em;
            color: var(--dark-text);
        }
        .cta-text strong {
            color: var(--primary-color);
        }


        /* Contact Section */
        .contact-section {
            background-color: var(--card-bg);
            padding: 50px 20px;
            text-align: center;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            margin-top: 40px;
        }

        .contact-section h2 {
            margin-bottom: 30px;
        }

        .contact-info {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
        }

        .contact-info a {
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 10px;
            padding: 15px 25px;
            text-decoration: none;
            color: white;
            border-radius: 30px; /* Pill shape */
            font-weight: bold;
            font-size: 1.1em;
            transition: background-color var(--transition-speed), transform var(--transition-speed);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            min-width: 280px; /* Ensure buttons are wide enough */
        }

        .contact-info a i {
            margin-right: 10px;
            font-size: 1.3em;
        }

        .email-btn {
            background-color: var(--primary-color);
        }
        
        .email-btn:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
        }

        .whatsapp-btn {
            background-color: var(--secondary-color);
        }

        .whatsapp-btn:hover {
            background-color: #1e7e34;
            transform: translateY(-3px);
        }
        
        /* Footer */
        footer {
            text-align: center;
            padding: 30px 20px;
            font-size: 0.9em;
            color: var(--light-text);
            margin-top: 50px;
            border-top: 1px solid #e9ecef;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero-section h1 {
                font-size: 2.5em;
            }
            .hero-section p {
                font-size: 1.1em;
            }
            h2 {
                font-size: 2em;
            }
            .feature-grid {
                grid-template-columns: 1fr; /* Single column on small screens */
            }
            .feature-card {
                padding: 25px;
            }
            .contact-info a {
                min-width: unset; /* Remove min-width on small screens */
                width: 90%; /* Take up more width */
            }
        }

        @media (max-width: 480px) {
            .hero-section {
                padding: 60px 15px;
            }
            .hero-section h1 {
                font-size: 2em;
            }
            .hero-section p {
                font-size: 1em;
            }
            h2 {
                font-size: 1.8em;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        
        <section class="hero-section">
            <div class="hero-content">
                <div class="profile-image-container">
                    <img src="yassine_saldani_profile.jpg" alt="Yassine Saldani Profile" class="profile-image">
                </div>
                <h1>Yassine Saldani</h1>
                <p>
                    <strong>Computer Programmer specializing in software development and IT solutions.</strong>
                    <br>
                    I transform innovative ideas into robust applications and cutting-edge technical solutions to enhance business efficiency.
                </p>
            </div>
        </section>

        <section class="features-section">
            <h2>My Expertise & Services</h2>
            <div class="feature-grid">
                
                <div class="feature-card">
                    <span class="icon-wrapper"><i class="fas fa-laptop-code"></i></span>
                    <h3>Custom Software Development</h3>
                    <p>Building powerful and reliable web and desktop applications tailored precisely to your unique business requirements.</p>
                </div>
                
                <div class="feature-card">
                    <span class="icon-wrapper"><i class="fas fa-cogs"></i></span>
                    <h3>Integrated IT Solutions</h3>
                    <p>
